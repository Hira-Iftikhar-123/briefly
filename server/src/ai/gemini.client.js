async function summarizeMeeting(notes) {
  // Placeholder until Gemini API integration is added.
  return {
    summary: `Summary placeholder for notes length ${notes?.length || 0}.`,
    actionItems: [],
  };
}

module.exports = { summarizeMeeting };
